const entityConfigurations = [];
export const getEntityConfiguration = index => entityConfigurations[index];
export const setEntityConfiguration = entityConfiguration => entityConfigurations.push(entityConfiguration) - 1;
